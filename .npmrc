node-linker=hoisted
shamefully-hoist=true
# 移除 ignore-scripts=true：pnpm 10 默认就不跑依赖的 build 脚本（除非白名单/approve），
# 保留同等安全；根项目 postinstall（见 package.json）借此自动补装 electron 二进制。
# @vscode/ripgrep/node-pty 等均为预编译或运行时下载，不受影响。
electron_mirror=https://npmmirror.com/mirrors/electron/
confirmModulesPurge=false
